# Extract the first and last elements of already created vector from,storing them as a new object

v <-c(2,3,4,5,6)
print(v)
first_ele <- v[1]
print(first_ele)
last_ele<- v[length(v)]
print(last_ele)
